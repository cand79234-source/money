# Neon 数据库连接串（在 Neon 控制台 Projects → 你的项目 → Connection Details 复制）
# 形如：postgresql://user:password@ep-xxxx-pooler.region.aws.neon.tech/neondb?sslmode=require
DATABASE_URL=

# 可选：多实例隔离（默认 default，单人使用无需改动）
# STATE_ID=default
